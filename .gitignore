node_modules/
dist/
.vite/
.DS_Store
Thumbs.db

.env
.env.*.local
.env.local

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vscode/
.idea/
*.code-workspace
*.swp
*.swo

coverage/
.nyc_output/

# build outputs
*.tsbuildinfo

# 3D-render intermediate output — atlas under public/atlas/ is committed,
# but the per-frame raw PNGs are not.
assets/atlas/raw/
# Source ZIP archives — extracted content under assets/source/<name>/ is
# committed, but the redundant zip is not.
assets/source/*.zip
